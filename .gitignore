node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.env*
!.env.example

# The workbook is committed; its extracted JSON is generated by
# `pnpm run import:cost-master` and committed too, so the app builds without
# the spreadsheet toolchain.
